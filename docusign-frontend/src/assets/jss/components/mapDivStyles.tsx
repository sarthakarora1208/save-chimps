import { createStyles, Theme } from '@material-ui/core';

const mapDivStyles = (theme: Theme) =>
  createStyles({
    root: {
      padding: '0',
      margin: '0',
      minHeight: '500px',
      height: '100%',
      width: '100%',
    },
  });

export default mapDivStyles;
